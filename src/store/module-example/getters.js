export function userId(state) {
    return state.user.id
}

export function userName(state) {
    return state.user.name
}

export function penmateId(state) {
    return state.penmate.id
}

export function penmateName(state) {
    return state.penmate.name
}

export function room(state) {
    return state.room
}